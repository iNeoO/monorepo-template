{
	"name": "create-monorepo-template",
	"version": "0.1.0",
	"type": "module",
	"description": "Scaffold a new monorepo from the monorepo-template starter",
	"bin": {
		"create-monorepo-template": "dist/index.js"
	},
	"files": [
		"dist",
		"templates"
	],
	"scripts": {
		"build": "rm -rf dist && tsc",
		"dev": "tsc --watch",
		"lint": "biome check ."
	},
	"dependencies": {
		"@inquirer/prompts": "^7.4.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.10",
		"@types/node": "^25.9.1",
		"typescript": "^6.0.3"
	}
}
